# 项目推荐与规划（Java × Python Agent 融合）

> 结合你的 [项目设计需求说明](需求文档/项目设计需求说明.txt)、[岗位技术要求文档](岗位技术要求文档.md)、以及你写的"有含金量"三条标准，给出 8 个候选 + 1 个首选 + 17 天落地排期。
> 生成日期：2026-09-04

---

## 一、先对齐："有含金量"翻译成可执行标准

你写的三条，我提炼成面试官视角下的硬判据，后面每个项目都用它打分：

| 你的标准 | 面试官真正在验证什么 | 项目里的落点 |
|---|---|---|
| 1. 明确场景与用户，解决真实问题（不是凭空想象） | 你懂不懂业务、有没有产品 sense | 选一个"痛点真实存在"的行业，且 Agent 切的是核心决策而非边缘功能 |
| 2. 完整生命周期 | 你是不是只会写代码，还是会做工程 | 需求→ER 设计→编码→测试→部署→压测→文档，全套留痕 |
| 3. 多种解决方案（可复用） | **这是核心**：你遇到问题怎么处理，方案能不能迁移 | 超卖/并发扣减、海量查询优化、单点故障、缓存一致性——每个都抽象成"套路" |

一句话判据：**面试官能不能沿着你的项目连续追问 30 分钟，且你都答得上来。** 图书管理系统 5 分钟就没话问；有并发、有决策、有 trade-off 的项目，问得越深你展示越多。你说的"可以借鉴 GitHub"我完全认同——本项目不必从零设计，站在成熟项目肩膀上做**智能化改造**。

---

## 二、候选池：8 个项目全览

| # | 项目 | 类型 | Java 侧硬核点 | Python Agent 分量 | 双岗适配 | 17 天风险 |
|---|---|---|---|---|---|---|
| 1 | **AI-WMS 智能仓储** ⭐首选 | 物流·路径一 | 并发扣减/超卖、缓存一致性、事务 | 预测 + 货位分配 + 拣货路径（三重决策，切主流程） | 极高 | 低（可锁 MVP） |
| 2 | **AI-TMS 智能配送调度** 强二号 | 物流·路径一 | 派单竞争、订单状态机、实时轨迹 | 车辆调度 + 路径规划 + ETA（调度=编排，命中 JD"调度编排"） | 极高 | 中 |
| 3 | 智能风控审批系统 | 金融·路径一 | 状态机、事务、审批流、幂等 | 信用评分 + 欺诈检测 + 自动审批 | 高 | 中（数据全模拟） |
| 4 | AI 智能排班系统 | 企业·路径一 | 班次/规则（并发弱） | 约束优化排班（分量重，偏算法） | 中（Java 弱） | 中 |
| 5 | 智能选课系统 | 校园·路径一 | 抢课超卖、冲突检测、并发 | 课表推荐 + 冲突检测 + 意愿预测 | 高 | 低（差异化弱） |
| 6 | 图书馆座位预约 + 推荐 | 校园·路径一 | 超卖/锁定/分布式锁/海量模糊查询 | 座位推荐 + 人流预测（偏薄） | 中高 | 低 |
| 7 | 工作流引擎复现 + 智能审批 | 平台·路径二 | 状态机、引擎架构、设计模式 | 智能审批、异常路由 | 高 | 高（17 天紧） |
| 8 | 分布式调度框架复现 + 智能调度 | 平台·路径二 | 调度器/执行器/任务分片/故障转移 | 智能优先级、负载预测 | 高 | 高 |

**探索过但排除**（省得走弯路）：
- ❌ 纯"智能体平台/知识库/RAG 问答"：最贴 Python JD 字面，但没有"Java 经典业务底座"，设计文档的融合架构不成立，投 Java 岗直接没故事。
- ❌ AI 客服/工单机器人：设计文档明确否掉的"浅层 AI 客服"，Agent 切的是边缘功能。

---

## 三、逐一点评（重点讲前 4 个）

- **#1 AI-WMS**：Java 的"库存超卖"是最经典、最好讲透的并发案例；Agent 三重决策都切主流程。（详见下节）
- **#2 AI-TMS**：最强替代者。Agent 的"调度"直接命中 Python JD 的"调度编排"关键词，配送调度天然就是编排（订单→车辆→路线→司机）。**主攻 Python 岗可选它**。弱点：Java 侧"派单竞争"的并发故事不如 WMS 的"库存超卖"经典（超卖是八股高频题，派单竞争不是）。
- **#3 智能风控审批**：最贴 Python JD 的"决策/风险"味道，但 JD 明确写"非算法岗"，风控易被面试官当算法岗追问 ML 细节（特征/模型/评估），没真实数据、全模拟，一问就虚。高收益高风险。
- **#4 智能排班**：Agent（约束优化排班）分量重，但 Java 侧几乎无并发，投 Java 岗失衡。
- **#5 选课 / #6 座位预约**：贴近生活、并发强、落地快，但差异化弱（应届生简历太常见），Agent 分量偏薄，Python 岗故事撑不住。
- **#7 / #8 开源复现**：工程深度最高，但 17 天复现引擎/框架再嵌 Agent，烂尾风险大，留作秋招后进阶更合适。

---

## 四、最终推荐：AI-WMS 智能仓储管理系统

### 为什么在 8 个里仍是它（比较出来的结论）
1. **双岗适配唯一"极高"且两边都硬**：Java 有经典超卖并发，Python 有切主流程的三重决策。
2. **可复用解决方案密度最高**：超卖/缓存一致性/预测/路径优化，全是对标 JD 的高频题。
3. **没有致命坑**：不像风控有"算法岗"风险、不像排班 Java 失衡、不像复现会烂尾。
4. **面试官零门槛理解业务**，你的时间全花在讲技术决策上。

> 决策提示：**若你主攻 Python 岗倾向更重，可换 AI-TMS**（"调度编排"更贴 Python JD）；否则 AI-WMS 是 8 选 1 的最优解。

### 是什么 / 解决谁的问题
**场景**：一个中小电商/物流仓库，SKU 多、出入库频繁。真实痛点：库存不准（账实不符）、货位乱放（高频商品放得远）、拣货路线乱。
**用户**：仓库管理员、拣货员。这不是"AI 客服"式浅层智能化，而是 AI 切入**库存、货位、拣货**三大核心决策。

### 架构（Java 底座 + Python Agent）
```
[Vue3 管理后台]
      │ RESTful
[Spring Boot 业务底座]  ←→  [Python FastAPI Agent 服务]
  ├─ 商品/入库/出库/库存/货位      ├─ 库存动态预测（补货建议）
  ├─ 并发扣减、库存流水、事务       ├─ 智能货位分配（频率/重量/关联度评分）
  ├─ MyBatis + MySQL + Redis      ├─ 拣货路径优化（订单波次合并 + 路径）
  └─ RabbitMQ 投递异步任务 ────────→ SQLAlchemy + （LLM API 可选）
```
通信**双通道**：实时查询走 RESTful，异步计算（预测/路径）走 MQ——两种方式都覆盖，面试两问都有答案。

### 硬核问题（都是可复用解决方案）
1. **并发扣减 / 超卖**：出入库同时操作同一 SKU → 乐观锁（版本号）打底 + Redis 分布式锁兜底。
2. **缓存一致性**：热点库存放 Redis，"先更 DB 再删缓存 + 延迟双删"或 MQ 保证最终一致。
3. **库存预测**：移动平均 / 指数平滑（先算法后 LLM）预测补货点。
4. **货位分配**：出库频率 × 重量 × 关联商品做评分函数，高频靠前、重物靠下。
5. **拣货路径优化**：订单波次合并 + 贪心/最近邻 TSP 近似，减少行走距离。
6. **单点故障**：Agent 挂掉不影响出库主流程——降级为"人工/规则分配"，体现高可用。

### 映射 JD 技能（一份项目，两版简历）
- 投 **Java 岗** 讲：Spring Boot + MyBatis + MySQL 事务/索引 + Redis + RabbitMQ + 并发锁。
- 投 **Python 岗** 讲：FastAPI + asyncio + SQLAlchemy + Agent 调度编排 + LLM API 调用 + 服务治理。

### 可借鉴的 GitHub / 参考
- Java 底座工程结构：`macrozheng/mall`（SpringBoot + MyBatis + Redis + RabbitMQ，正好是推荐技术栈）
- 后台管理脚手架：`elunez/eladmin`（省去手写后台时间）
- Python Agent 编排参考：`langgenius/dify`（开源 LLM 平台，看 Agent 调度/编排思路）、`langchain-ai/langchain`
- WMS 领域知识：成熟开源 WMS 不多，业务模型（入库/波次/货位/盘点）参考仓储管理书籍与流程文档

---

## 五、里程碑排期（17 天开发 + 7 天准备 = 9/28 前完成）

> 硬约束：**9/21 起停止写代码，转入简历+面试准备**。宁可交付"小而完整"的 MVP，也不要"大而烂尾"的半成品。

| 日期 | 阶段 | 交付物 |
|---|---|---|
| 9/4–9/5（2 天） | 需求收敛 + 架构 + 接口契约 | 业务边界清单、架构图、ER 图、Java↔Agent 接口契约文档 |
| 9/6–9/11（6 天） | **Java 底座**（第一阶段） | 商品/入库/出库/库存/货位 5 个核心模块；并发扣减；MyBatis+MySQL+Redis；单元测试 |
| 9/12–9/13（2 天） | Java 自测 + 简单前端 | 接口自测通过；Vue3 后台（套 eladmin 模板）；确认不依赖 Agent 也能完整跑通 |
| 9/14–9/18（5 天） | **Python Agent**（第二阶段） | FastAPI 预测/货位分配/路径优化；通过预留接口联调接入 |
| 9/19–9/20（2 天） | 端到端 + 收尾 | 完整演示链路、异常兜底、日志、简单压测（并发扣减报告）、README + 技术文档 |
| 9/21–9/27（7 天） | **简历 + 面试准备** | 两版简历（Java/Python 各一）、项目讲解 STAR、八股（MySQL/Redis/并发/网络/OS） |

**给简历的一句话**（示例）："独立设计并实现 AI-WMS 智能仓储系统：Java（Spring Boot + MyBatis + Redis + RabbitMQ）承载高频出入库与并发一致性，Python（FastAPI + SQLAlchemy）Agent 承担库存预测、货位分配与拣货路径优化，RESTful + MQ 双通道联调，全链路可部署可演示并通过并发压测。"

---

## 附：两个关键提醒

1. **智能 ≠ 必须 LLM**。库存预测（指数平滑）、货位分配（评分函数）、路径优化（贪心/TSP 近似）都是纯算法，不需要 API key 就能跑、能演示、能讲原理。LLM（DeepSeek/通义/智谱等国产 API，很便宜）只作为"自然语言交互 / 异常决策"的加分项后置接入。
2. **范围控制是成败关键**。第 6–11 天如果 Java 底座没跑通，立刻砍掉"拣货路径优化"，只保留"预测 + 货位分配"——一个完整能演示的 WMS，远胜一个烂尾的大 WMS。
