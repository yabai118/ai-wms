# Day 02（9月5日 · 画 ER 图 + 定接口契约）

## 今日代码做了什么
（今天无代码，产出的是 ER 图和接口契约文档）

## 今日学到的知识
（⚠️ 用自己的话写，不要抄 Claude 原话）

### 知识点1：数据库三大范式（🔴）
- 是什么：1nf 字段不可再分，一个字段只能有一个值，2nf 非主键的外键完全依赖主键，3nf 非主键的外键不依赖其他非主键字段
- 为什么（原理）：
- 我能怎么跟面试官讲：

### 知识点2：主键 / 外键 / 索引设计（🔴）
- 是什么：主键就是唯一标识，像学号一样，只能靠这个找到你，外键就是其他字段
- 为什么（原理）：
- 我能怎么跟面试官讲：

### 知识点3：库存表为什么单独拆出来（🔴）
- 是什么：一行数据有多个字段，有高频修改的，有低频修改的，为了防止多人同时操作一个行导致数据覆盖，数据库只允许一行同时只能一个人操作，所以为了防止当有人在修改高频字段时有人又想查看低频字段却还要排队所以把低频字段和高频字段分开
- 为什么（原理）：
- 我能怎么跟面试官讲：

### 知识点4：RESTful + 幂等（🔴）
- 是什么：
- 为什么（原理）：
- 我能怎么跟面试官讲：

## 今日任务一：7 张表的字段设计（核心产出）

> 表名和关系我已经给出，你填每张表的**字段**。第一张"商品表"是完整示范，照着它的格式填其余 6 张。

**表间关系（先看懂）：**
- 商品 1 : N 库存（一个商品可放多个货位）
- 货位 1 : N 库存
- 入库单 1 : N 入库明细，入库明细 N : 1 商品
- 出库单 1 : N 出库明细，出库明细 N : 1 商品

**1. 商品表 product（示范，照这个格式）：**
- id：主键，自增 BIGINT
- sku_code：SKU 编码（唯一索引）
- name：商品名
- category：分类
- spec：规格
- weight：重量（货位分配用）
- volume：体积
- unit：单位
- price：单价
- created_at / updated_at

**2. 货位表 location**（提示：货位编码、区域、排、层、位、容量、状态）：
- 

**3. 库存表 stock**（提示：product_id、location_id、quantity，**别忘了 version 乐观锁字段**）：
- 

**4. 入库单 inbound_order**（提示：单号唯一、供应商、状态、时间）：
- 

**5. 入库明细 inbound_order_item**（提示：入库单 id、商品 id、数量、货位 id）：
- 

**6. 出库单 outbound_order**（提示：单号唯一、状态、时间）：
- 

**7. 出库明细 outbound_order_item**（提示：出库单 id、商品 id、数量、货位 id）：
- 

## 今日任务二：Java ↔ Agent 接口契约

> 三个 Agent 接口，你填每个接口的**入参 / 出参**字段。

**① 库存预测** POST /agent/predict
- 入参：
- 出参：

**② 货位分配** POST /agent/allocate
- 入参：
- 出参：

**③ 拣货路径** POST /agent/pick-path
- 入参：
- 出参：

## 今日面试题（背下来）
**Q1：数据库三大范式是什么？**
- 我的答案要点：

**Q2：为什么有时候要"反范式"（冗余）？**
- 我的答案要点：

**Q3：什么是幂等？出库扣库存怎么保证幂等？**
- 我的答案要点：

## 踩过的坑
- 

## 明天要问 Claude 的问题
-
