# 岗位技术要求文档

> 基于「Java 研发工程师」与「Python 研发工程师」两份 JD 提炼，服务于秋招项目选型与学习规划。
> 生成日期：2026-09-04

---

## 一、核心结论（先读这一段）

两份 JD 表面是不同语言，但底层有一大块**共同的后端工程能力**，这才是秋招真正的硬通货。你的项目必须同时覆盖三块：

1. **共同底座（一次投入、两岗通用，性价比最高）**：MySQL / Redis / MQ / 分布式与并发基础 / 接口设计 / 工程规范。
2. **Java 专属**：Spring Boot + Spring Cloud 微服务 + MyBatis。
3. **Python 专属**：FastAPI + asyncio + SQLAlchemy + LLM / 智能体。

你的融合项目（Java 业务底座 + Python 智能 Agent）恰好能**一次性覆盖这三块**——这正是你 [项目设计需求说明](需求文档/项目设计需求说明.txt) 里最聪明的地方：一个项目，同时投 Java 和 Python 两个岗位都有话说。

---

## 二、共同技术底座（两岗都要求，最高优先级）

| 技能 | JD 依据（原文） | 项目落地方式 |
|---|---|---|
| 数据结构与算法 | Java："掌握数据结构、算法、操作系统原理" | 库存预测、拣货路径优化、并发扣减 |
| 关系型数据库 MySQL | 两岗都写：索引原理、事务隔离级别、查询优化 | 库存/订单表设计、慢查询优化、事务与锁 |
| 缓存 Redis | Java："redis"；Python："Redis 等 NoSQL 缓存" | 热点库存缓存、分布式锁、防止缓存穿透/击穿 |
| 消息队列 MQ | Java："mq"；Python："RocketMQ、Kafka 基本使用场景" | Java→Agent 任务投递、异步削峰、解耦 |
| HTTP 协议 / 接口设计 | Java："熟悉 HTTP 协议及 W3C"；Python："接口设计" | RESTful 契约、状态码、幂等、错误码 |
| 版本控制 / 构建 | Java："Git、maven" | 全程 Git 规范提交 + Maven |
| 分布式基础 | Python："分布式系统常见挑战及解决方案" | 一致性、可用性、限流、幂等 |

> 面试里这 7 项几乎 100% 会被问到，且两岗通吃，是时间投入回报最高的部分。

---

## 三、Java 研发工程师 · 专属技能栈

| 分层 | 内容 | 优先级 |
|---|---|---|
| 语言核心 | 集合、并发（synchronized/ReentrantLock/CAS/线程池）、JVM（GC、内存模型） | 高 |
| Web 基础 | Servlet / JSP（了解即可，已偏 legacy）、HTTP 协议 | 中 |
| 框架 | Spring、SpringMVC、**Spring Boot**、Spring Cloud（注册中心/网关/配置/熔断） | 高 |
| ORM | **MyBatis**（JD 写的 iBatis 即其前身）/ Hibernate | 高 |
| 数据库 | MySQL + Oracle（Oracle 了解即可，先吃透 MySQL） | 高 |
| 前端 | HTML/CSS/JS + 一个框架（**建议 Vue**，国内岗位主流；React 亦可） | 中 |
| 工程 | Maven、Git、SVN、单元测试 | 中 |

---

## 四、Python 研发工程师 · 专属技能栈

| 分层 | 内容 | 优先级 |
|---|---|---|
| 语言核心 | 多线程 vs GIL、**异步 IO（asyncio/await）**、装饰器、上下文管理器、生成器 | 高 |
| Web 框架 | **FastAPI**（首选：原生异步 + 类型提示，正好对应 JD 的 asyncio）/ Django / Flask | 高 |
| ORM | **SQLAlchemy**（JD 点名） | 高 |
| 数据库 | MySQL / PostgreSQL + Redis | 高 |
| 分布式 | 消息队列（RocketMQ/Kafka）、服务治理（Nacos/Sentinel，了解即可） | 中 |
| AI 工程化 | **LLM API 调用**、智能体框架（LangChain/AutoGen 等）、AI 编程助手深度使用（Cursor/ClaudeCode）——**这是该 JD 的差异化加分项** | 加分 |

> 注意：Python JD 明确说"非算法类岗位，专注后端工程"。所以你的 Agent 核心要落在**工程**（服务架构、调度编排、性能优化），而非训练模型。

---

## 五、技能重叠区与学习优先级

### 重叠区（性价比最高，优先吃透）
- **MySQL**：索引、事务、隔离级别、SQL 优化 —— 两岗必问
- **Redis**：缓存三大问题（穿透/击穿/雪崩）、分布式锁、持久化
- **MQ**：异步、削峰、解耦、消息可靠性（不丢/不重）
- **接口设计**：RESTful、幂等、版本、错误码
- **并发与分布式一致性**

### 按 3 周时间的优先级分配
1. （贯穿全程）MySQL + Redis + 接口设计 + Git 工程规范
2. Java 主线：Spring Boot → MyBatis → 并发与锁 → 轻量微服务（网关/注册中心用现成组件，不必手写）
3. Python 主线：FastAPI + asyncio + SQLAlchemy → Agent 决策算法 → LLM API（加分，可后置）

---

## 六、一句话结论（给你的项目定标准）

> 项目必须让面试官能「沿着简历一路问下去」：数据库怎么设计的？并发怎么解决的？为什么用 MQ？Java 和 Agent 怎么通信？预测算法怎么选的？——**每个问题你都能答上来，这个项目就有含金量。**

**建议技术选型：**
- Java 侧：Spring Boot + MyBatis + MySQL + Redis + RabbitMQ（或 RocketMQ）
- Python 侧：FastAPI + SQLAlchemy + （LLM API 可选）
- 前端：Vue3 + Element Plus（或现成 admin 模板，省时间）
- 通信：Java ↔ Python 用 **RESTful（联调简单）+ MQ（异步任务）双通道**，两种方式都覆盖到，面试都有话说
